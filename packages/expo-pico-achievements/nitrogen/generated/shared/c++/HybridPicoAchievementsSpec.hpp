///
/// HybridPicoAchievementsSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `Achievement` to properly resolve imports.
namespace margelo::nitro::expopico::achievements { struct Achievement; }
// Forward declaration of `UnlockAchievementResult` to properly resolve imports.
namespace margelo::nitro::expopico::achievements { struct UnlockAchievementResult; }
// Forward declaration of `AddCountResult` to properly resolve imports.
namespace margelo::nitro::expopico::achievements { struct AddCountResult; }
// Forward declaration of `AddBitfieldResult` to properly resolve imports.
namespace margelo::nitro::expopico::achievements { struct AddBitfieldResult; }
// Forward declaration of `AchievementUnlockedEvent` to properly resolve imports.
namespace margelo::nitro::expopico::achievements { struct AchievementUnlockedEvent; }

#include <string>
#include "Achievement.hpp"
#include <vector>
#include <NitroModules/Promise.hpp>
#include "UnlockAchievementResult.hpp"
#include "AddCountResult.hpp"
#include "AddBitfieldResult.hpp"
#include "AchievementUnlockedEvent.hpp"
#include <functional>

namespace margelo::nitro::expopico::achievements {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `PicoAchievements`
   * Inherit this class to create instances of `HybridPicoAchievementsSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridPicoAchievements: public HybridPicoAchievementsSpec {
   * public:
   *   HybridPicoAchievements(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridPicoAchievementsSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridPicoAchievementsSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridPicoAchievementsSpec() override = default;

    public:
      // Properties
      virtual bool getAvailable() = 0;
      virtual std::string getSdkVersion() = 0;

    public:
      // Methods
      virtual std::shared_ptr<Promise<std::vector<Achievement>>> getAllAchievements() = 0;
      virtual std::shared_ptr<Promise<std::vector<Achievement>>> getUnlockedAchievements() = 0;
      virtual std::shared_ptr<Promise<std::vector<Achievement>>> getAchievementProgress(const std::vector<std::string>& apiNames) = 0;
      virtual std::shared_ptr<Promise<UnlockAchievementResult>> unlockAchievement(const std::string& apiName) = 0;
      virtual std::shared_ptr<Promise<AddCountResult>> addAchievementCount(const std::string& apiName, double count) = 0;
      virtual std::shared_ptr<Promise<AddBitfieldResult>> addAchievementBitfield(const std::string& apiName, const std::string& bitfield) = 0;
      virtual double addAchievementUnlockedListener(const std::function<void(const AchievementUnlockedEvent& /* event */)>& listener) = 0;
      virtual void removeAchievementUnlockedListener(double id) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "PicoAchievements";
  };

} // namespace margelo::nitro::expopico::achievements
