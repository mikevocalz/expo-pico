///
/// PicoContainerType.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::expopico::spatial {

  /**
   * An enum which can be represented as a JavaScript union (PicoContainerType).
   */
  enum class PicoContainerType {
    WINDOW_CONTAINER      SWIFT_NAME(windowContainer) = 0,
    STAGE      SWIFT_NAME(stage) = 1,
    NONE      SWIFT_NAME(none) = 2,
  } CLOSED_ENUM;

} // namespace margelo::nitro::expopico::spatial

namespace margelo::nitro {

  // C++ PicoContainerType <> JS PicoContainerType (union)
  template <>
  struct JSIConverter<margelo::nitro::expopico::spatial::PicoContainerType> final {
    static inline margelo::nitro::expopico::spatial::PicoContainerType fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("window-container"): return margelo::nitro::expopico::spatial::PicoContainerType::WINDOW_CONTAINER;
        case hashString("stage"): return margelo::nitro::expopico::spatial::PicoContainerType::STAGE;
        case hashString("none"): return margelo::nitro::expopico::spatial::PicoContainerType::NONE;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum PicoContainerType - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::expopico::spatial::PicoContainerType arg) {
      switch (arg) {
        case margelo::nitro::expopico::spatial::PicoContainerType::WINDOW_CONTAINER: return JSIConverter<std::string>::toJSI(runtime, "window-container");
        case margelo::nitro::expopico::spatial::PicoContainerType::STAGE: return JSIConverter<std::string>::toJSI(runtime, "stage");
        case margelo::nitro::expopico::spatial::PicoContainerType::NONE: return JSIConverter<std::string>::toJSI(runtime, "none");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert PicoContainerType to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("window-container"):
        case hashString("stage"):
        case hashString("none"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
