///
/// RoomConnectionState.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::expopico::rooms {

  /**
   * An enum which can be represented as a JavaScript union (RoomConnectionState).
   */
  enum class RoomConnectionState {
    DISCONNECTED      SWIFT_NAME(disconnected) = 0,
    CONNECTING      SWIFT_NAME(connecting) = 1,
    JOINED      SWIFT_NAME(joined) = 2,
    LEAVING      SWIFT_NAME(leaving) = 3,
    ERROR      SWIFT_NAME(error) = 4,
  } CLOSED_ENUM;

} // namespace margelo::nitro::expopico::rooms

namespace margelo::nitro {

  // C++ RoomConnectionState <> JS RoomConnectionState (union)
  template <>
  struct JSIConverter<margelo::nitro::expopico::rooms::RoomConnectionState> final {
    static inline margelo::nitro::expopico::rooms::RoomConnectionState fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("disconnected"): return margelo::nitro::expopico::rooms::RoomConnectionState::DISCONNECTED;
        case hashString("connecting"): return margelo::nitro::expopico::rooms::RoomConnectionState::CONNECTING;
        case hashString("joined"): return margelo::nitro::expopico::rooms::RoomConnectionState::JOINED;
        case hashString("leaving"): return margelo::nitro::expopico::rooms::RoomConnectionState::LEAVING;
        case hashString("error"): return margelo::nitro::expopico::rooms::RoomConnectionState::ERROR;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum RoomConnectionState - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::expopico::rooms::RoomConnectionState arg) {
      switch (arg) {
        case margelo::nitro::expopico::rooms::RoomConnectionState::DISCONNECTED: return JSIConverter<std::string>::toJSI(runtime, "disconnected");
        case margelo::nitro::expopico::rooms::RoomConnectionState::CONNECTING: return JSIConverter<std::string>::toJSI(runtime, "connecting");
        case margelo::nitro::expopico::rooms::RoomConnectionState::JOINED: return JSIConverter<std::string>::toJSI(runtime, "joined");
        case margelo::nitro::expopico::rooms::RoomConnectionState::LEAVING: return JSIConverter<std::string>::toJSI(runtime, "leaving");
        case margelo::nitro::expopico::rooms::RoomConnectionState::ERROR: return JSIConverter<std::string>::toJSI(runtime, "error");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert RoomConnectionState to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("disconnected"):
        case hashString("connecting"):
        case hashString("joined"):
        case hashString("leaving"):
        case hashString("error"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
