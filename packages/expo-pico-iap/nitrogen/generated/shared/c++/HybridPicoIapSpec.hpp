///
/// HybridPicoIapSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `IapProduct` to properly resolve imports.
namespace margelo::nitro::expopico::iap { struct IapProduct; }
// Forward declaration of `ConsumeResult` to properly resolve imports.
namespace margelo::nitro::expopico::iap { struct ConsumeResult; }
// Forward declaration of `IapPurchase` to properly resolve imports.
namespace margelo::nitro::expopico::iap { struct IapPurchase; }
// Forward declaration of `PurchaseResult` to properly resolve imports.
namespace margelo::nitro::expopico::iap { struct PurchaseResult; }

#include <string>
#include "IapProduct.hpp"
#include <vector>
#include <NitroModules/Promise.hpp>
#include "ConsumeResult.hpp"
#include "IapPurchase.hpp"
#include "PurchaseResult.hpp"

namespace margelo::nitro::expopico::iap {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `PicoIap`
   * Inherit this class to create instances of `HybridPicoIapSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridPicoIap: public HybridPicoIapSpec {
   * public:
   *   HybridPicoIap(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridPicoIapSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridPicoIapSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridPicoIapSpec() override = default;

    public:
      // Properties
      virtual bool getAvailable() = 0;
      virtual std::string getSdkVersion() = 0;

    public:
      // Methods
      virtual std::shared_ptr<Promise<std::vector<IapProduct>>> getProducts(const std::vector<std::string>& skus) = 0;
      virtual std::shared_ptr<Promise<ConsumeResult>> consumePurchase(const std::string& purchaseToken) = 0;
      virtual std::shared_ptr<Promise<std::vector<IapPurchase>>> getPurchaseHistory() = 0;
      virtual std::shared_ptr<Promise<PurchaseResult>> purchase(const std::string& sku) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "PicoIap";
  };

} // namespace margelo::nitro::expopico::iap
