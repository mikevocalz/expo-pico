///
/// StorageConflictPolicy.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::expopico::storage {

  /**
   * An enum which can be represented as a JavaScript union (StorageConflictPolicy).
   */
  enum class StorageConflictPolicy {
    SERVER_WINS      SWIFT_NAME(serverWins) = 0,
    CLIENT_WINS      SWIFT_NAME(clientWins) = 1,
    MANUAL      SWIFT_NAME(manual) = 2,
  } CLOSED_ENUM;

} // namespace margelo::nitro::expopico::storage

namespace margelo::nitro {

  // C++ StorageConflictPolicy <> JS StorageConflictPolicy (union)
  template <>
  struct JSIConverter<margelo::nitro::expopico::storage::StorageConflictPolicy> final {
    static inline margelo::nitro::expopico::storage::StorageConflictPolicy fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("server-wins"): return margelo::nitro::expopico::storage::StorageConflictPolicy::SERVER_WINS;
        case hashString("client-wins"): return margelo::nitro::expopico::storage::StorageConflictPolicy::CLIENT_WINS;
        case hashString("manual"): return margelo::nitro::expopico::storage::StorageConflictPolicy::MANUAL;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum StorageConflictPolicy - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::expopico::storage::StorageConflictPolicy arg) {
      switch (arg) {
        case margelo::nitro::expopico::storage::StorageConflictPolicy::SERVER_WINS: return JSIConverter<std::string>::toJSI(runtime, "server-wins");
        case margelo::nitro::expopico::storage::StorageConflictPolicy::CLIENT_WINS: return JSIConverter<std::string>::toJSI(runtime, "client-wins");
        case margelo::nitro::expopico::storage::StorageConflictPolicy::MANUAL: return JSIConverter<std::string>::toJSI(runtime, "manual");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert StorageConflictPolicy to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("server-wins"):
        case hashString("client-wins"):
        case hashString("manual"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
