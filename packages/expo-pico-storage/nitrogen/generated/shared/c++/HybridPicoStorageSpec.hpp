///
/// HybridPicoStorageSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `StorageStatus` to properly resolve imports.
namespace margelo::nitro::expopico::storage { enum class StorageStatus; }
// Forward declaration of `StorageSaveResult` to properly resolve imports.
namespace margelo::nitro::expopico::storage { struct StorageSaveResult; }
// Forward declaration of `StorageEntryType` to properly resolve imports.
namespace margelo::nitro::expopico::storage { enum class StorageEntryType; }
// Forward declaration of `StorageSaveOptions` to properly resolve imports.
namespace margelo::nitro::expopico::storage { struct StorageSaveOptions; }
// Forward declaration of `StorageLoadResult` to properly resolve imports.
namespace margelo::nitro::expopico::storage { struct StorageLoadResult; }
// Forward declaration of `StorageSyncResult` to properly resolve imports.
namespace margelo::nitro::expopico::storage { struct StorageSyncResult; }
// Forward declaration of `StorageQuota` to properly resolve imports.
namespace margelo::nitro::expopico::storage { struct StorageQuota; }
// Forward declaration of `StorageConflictEvent` to properly resolve imports.
namespace margelo::nitro::expopico::storage { struct StorageConflictEvent; }
// Forward declaration of `StorageSyncProgressEvent` to properly resolve imports.
namespace margelo::nitro::expopico::storage { struct StorageSyncProgressEvent; }

#include <string>
#include "StorageStatus.hpp"
#include "StorageSaveResult.hpp"
#include <NitroModules/Promise.hpp>
#include "StorageEntryType.hpp"
#include "StorageSaveOptions.hpp"
#include <optional>
#include "StorageLoadResult.hpp"
#include <vector>
#include "StorageSyncResult.hpp"
#include "StorageQuota.hpp"
#include "StorageConflictEvent.hpp"
#include <functional>
#include "StorageSyncProgressEvent.hpp"

namespace margelo::nitro::expopico::storage {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `PicoStorage`
   * Inherit this class to create instances of `HybridPicoStorageSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridPicoStorage: public HybridPicoStorageSpec {
   * public:
   *   HybridPicoStorage(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridPicoStorageSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridPicoStorageSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridPicoStorageSpec() override = default;

    public:
      // Properties
      virtual bool getAvailable() = 0;
      virtual std::string getSdkVersion() = 0;
      virtual StorageStatus getStatus() = 0;

    public:
      // Methods
      virtual std::shared_ptr<Promise<StorageSaveResult>> saveEntry(const std::string& key, const std::string& value, StorageEntryType type, const std::optional<StorageSaveOptions>& options) = 0;
      virtual std::shared_ptr<Promise<StorageLoadResult>> loadEntry(const std::string& key) = 0;
      virtual std::shared_ptr<Promise<void>> deleteEntry(const std::string& key) = 0;
      virtual std::shared_ptr<Promise<std::vector<std::string>>> listKeys() = 0;
      virtual std::shared_ptr<Promise<StorageSyncResult>> syncStorage() = 0;
      virtual std::shared_ptr<Promise<StorageQuota>> getStorageQuota() = 0;
      virtual std::shared_ptr<Promise<void>> clearLocalCache() = 0;
      virtual double addStorageConflictListener(const std::function<void(const StorageConflictEvent& /* event */)>& listener) = 0;
      virtual double addStorageSyncProgressListener(const std::function<void(const StorageSyncProgressEvent& /* event */)>& listener) = 0;
      virtual double addStorageSyncCompleteListener(const std::function<void(const StorageSyncResult& /* result */)>& listener) = 0;
      virtual void removeListener(double id) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "PicoStorage";
  };

} // namespace margelo::nitro::expopico::storage
