///
/// EntitlementStatus.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::expopico::subscription {

  /**
   * An enum which can be represented as a JavaScript union (EntitlementStatus).
   */
  enum class EntitlementStatus {
    ACTIVE      SWIFT_NAME(active) = 0,
    IN_GRACE      SWIFT_NAME(inGrace) = 1,
    PAUSED      SWIFT_NAME(paused) = 2,
    CANCELLED      SWIFT_NAME(cancelled) = 3,
    EXPIRED      SWIFT_NAME(expired) = 4,
    NOT_SUBSCRIBED      SWIFT_NAME(notSubscribed) = 5,
  } CLOSED_ENUM;

} // namespace margelo::nitro::expopico::subscription

namespace margelo::nitro {

  // C++ EntitlementStatus <> JS EntitlementStatus (union)
  template <>
  struct JSIConverter<margelo::nitro::expopico::subscription::EntitlementStatus> final {
    static inline margelo::nitro::expopico::subscription::EntitlementStatus fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("active"): return margelo::nitro::expopico::subscription::EntitlementStatus::ACTIVE;
        case hashString("in-grace"): return margelo::nitro::expopico::subscription::EntitlementStatus::IN_GRACE;
        case hashString("paused"): return margelo::nitro::expopico::subscription::EntitlementStatus::PAUSED;
        case hashString("cancelled"): return margelo::nitro::expopico::subscription::EntitlementStatus::CANCELLED;
        case hashString("expired"): return margelo::nitro::expopico::subscription::EntitlementStatus::EXPIRED;
        case hashString("not-subscribed"): return margelo::nitro::expopico::subscription::EntitlementStatus::NOT_SUBSCRIBED;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum EntitlementStatus - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::expopico::subscription::EntitlementStatus arg) {
      switch (arg) {
        case margelo::nitro::expopico::subscription::EntitlementStatus::ACTIVE: return JSIConverter<std::string>::toJSI(runtime, "active");
        case margelo::nitro::expopico::subscription::EntitlementStatus::IN_GRACE: return JSIConverter<std::string>::toJSI(runtime, "in-grace");
        case margelo::nitro::expopico::subscription::EntitlementStatus::PAUSED: return JSIConverter<std::string>::toJSI(runtime, "paused");
        case margelo::nitro::expopico::subscription::EntitlementStatus::CANCELLED: return JSIConverter<std::string>::toJSI(runtime, "cancelled");
        case margelo::nitro::expopico::subscription::EntitlementStatus::EXPIRED: return JSIConverter<std::string>::toJSI(runtime, "expired");
        case margelo::nitro::expopico::subscription::EntitlementStatus::NOT_SUBSCRIBED: return JSIConverter<std::string>::toJSI(runtime, "not-subscribed");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert EntitlementStatus to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("active"):
        case hashString("in-grace"):
        case hashString("paused"):
        case hashString("cancelled"):
        case hashString("expired"):
        case hashString("not-subscribed"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
